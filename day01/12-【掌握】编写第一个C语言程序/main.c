// 引入系统头文件，相当于C语言系统的工具箱。
#include <stdio.h>

/*
    1.main函数是程序执行的入口，每一个可以执行的C语言程序，有且只有一个main().
    2.如果没有main(),那么程序无法执行。
    3.main函数的格式不能改变。
 
    C语言当中，每一句代码都是以“;”结束。
 */
int main(){
    // 打印出hello world。
    printf("hello world222!\n");
    ;// 空语句
    // 表示程序正常结束
    return 0;
    
}
